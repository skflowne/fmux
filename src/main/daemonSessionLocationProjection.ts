import { OrderedSessionLocationProjection } from '../shared/orderedSessionLocationProjection';

/** Main-process adapter name for the shared location lifecycle owner. */
export class DaemonSessionLocationProjection extends OrderedSessionLocationProjection {}
